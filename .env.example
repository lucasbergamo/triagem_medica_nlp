# Copie para .env. Valores abaixo são os defaults de src/utils/config.py.

SEED=42
LOG_LEVEL=INFO

# Backend de inferência selecionável em runtime: sklearn | onnx | onnx-int8
MODEL_BACKEND=sklearn

# Piso do gate de qualidade — macro-F1 mínimo para promover o modelo
MIN_MACRO_F1=0.50
